/** @type {import("prettier").Config} */
const config = {
  printWidth: 80,
  singleQuote: false,
  trailingComma: "es5",
  tabWidth: 2,
  useTabs: false,
  plugins: ["prettier-plugin-tailwindcss"],
};
export default config;
